specVersion: "v1beta"
serviceId: "bored"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "bored-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
